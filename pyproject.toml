[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "shark-adaptive-research"
version = "0.1.0"
description = "Adaptive quantitative research engine for multi-market strategy discovery and validation."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
