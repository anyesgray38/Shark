#!/bin/bash
# Shark — persistent memory Claude launcher
# Add to PATH: echo 'export PATH="$PATH:/home/user/Shark"' >> ~/.bashrc

export ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-sk-dummy}"
# Uncomment and set for Ollama:
# export ANTHROPIC_BASE_URL="http://localhost:8082"

exec python3 "$(dirname "$0")/memory_claude.py" "$@"
