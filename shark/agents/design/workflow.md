# Penpot Workflow

`requirement -> Design Agent -> Penpot specification -> implementation -> design validation`

If Penpot is not connected, produce `DESIGN_SPEC_ONLY` and do not claim a remote design change.
