# Shark Design System

Use semantic tokens for color, spacing, typography, radius, shadow, and motion. Prefer reusable components and variants for dashboard surfaces. Visual emphasis must never imply unearned statistical confidence.
