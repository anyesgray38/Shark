name: Shark Continuous Research

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: shark-continuous-research
  cancel-in-progress: false

jobs:
  research:
    runs-on: self-hosted
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify runner Python
        run: |
          python3 --version
          python3 -m pip --version

      - name: Install
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -e .
          python3 -m pip install pytest

      - name: Deterministic tests
        run: python3 -m pytest -q

      - name: Run research agents
        run: python3 -m shark.agents.daily

      - name: Validate research report
        run: |
          test -s reports/daily-agent-report.json
          python3 -m json.tool reports/daily-agent-report.json > /dev/null

      - name: Upload research report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shark-daily-research-${{ github.run_id }}
          path: reports/
          if-no-files-found: error
          retention-days: 30
